/**
 * 顶栏组件参数
 */
export interface TopbarProps {
  /**
   * @desc 控制Topbar固定在视窗。
   */
  isSticky?: boolean;
}

/**
 * 侧边栏组件参数
 */
export interface SiderbarProps {
  /**
   * @desc 控制Siderbar固定在视窗。
   */
  isSticky?: boolean;
}
